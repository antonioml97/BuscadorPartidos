 
#Lenguaje de la aplicacion
language: node_js
#Distinas versiones del lenguaje donde voy a probar los test
node_js:
  - "10"
  - "14"
#Instalo dependencias, gestor de tareas y ejecuto los tests.
build:
  ci:
    - npm install
    - npm install -g gulp
    - gulp test